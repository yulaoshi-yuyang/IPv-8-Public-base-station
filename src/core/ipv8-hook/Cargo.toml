[package]
name = "ipv8-hook"
description = "IPv8+ 外部判决钩子：本地 NDJSON/IPC，把数据包判决权交给任意外部程序（对标 NFQUEUE）"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"

# AGENTS.md —— 项目宪法（人和 AI 共同遵守）

0. 总纲
- 小完工 > 大烂尾。任何时刻 main 上必须有一个能跑的版本
- 复杂度增速 ≤ 理解增速。感觉看不懂自己的代码了 = 停手加厚地基
- 本文件是唯一行为标准，与它冲突的旧习惯一律作废

1. 立项书（动工前写完，放 docs/charter.md）
- 一句话定位
- v1 做什么：前三个功能，写死
- v1 不做什么：负面清单（最重要，塔全是被"顺手也能做"压塌的）
- 第一个里程碑：输入什么、输出什么、怎样算它活了
- 限高令：超出的功能进 docs/parking-lot.md，v1 交付后再审批

2. 实验田
- 新想法先进停车场观察 48 小时（48 小时规则），还想做才开实验田
- 实验田 24 小时内出结论（行/不行），不许长期养着
- 验证通过才合入 main，合入同时删除实验代码

3. 模块纪律（防胶水）
- 每个目录一个模块，模块之间只许走公开接口
- 禁止跨模块 import 内部文件
- 每个模块 README 末尾维护 import 白名单
- 想新增 import？先回答：能不能通过对方的公开接口解决？

4. 拆除纪律
- Boy Scout Rule：每次提交让代码比你来时好一点点，哪怕一行
- 每次 commit 自问：哪块积木拆了塔不会塌？有就顺手删
- 每月一次全量审查：死代码、废文件、过时文档，当场删
- 拆除和改动同一 commit，单独开"清理 commit"必烂尾

5. 索引维护
- 产物三层：
  1. 根 README：定位一句话、运行方式、目录地图（一目录一行）
  2. 每模块 README ≤30 行，固定三段：干什么 / 对外暴露什么 / 内部文件一行一个清单
  3. 对外文件头 3 行：是什么 / 输入 / 输出；内部文件不写头
- 只写代码里真实存在的，禁止推测；不写实现细节
- 依赖关系从 import 提取，列模块 README 末尾

6. 提交纪律
- message 格式：类型: 一句话（feat/fix/refactor/del/docs）
- 索引/文档与代码同一 commit
- 跑不起来的代码不许上 main
- 每天至少一个 commit（顺便防一天盖十层）

7. 里程碑制度（楼层）
- 每层楼必须同时满足：能跑、能演示、索引同步
- 每盖满三层，允许一次"加厚地基"重构，限时 1-2 天
- 重构期间冻结新功能

8. 依赖管理
- 新依赖 = 新承重墙，必须登记 docs/dependencies.md：名字、锁死版本、为什么需要
- 同类库只许存在一个（禁止三个 UI 框架式的平行宇宙）

9. AI 协作规则
- 新会话第一步：读 AGENTS.md + 根 README，自检索引是否失真
- AI 提议加功能时：先查停车场 → 再问地基承不承重 → 都过了才准动工
- AI 生成索引后必须跑验收清单：每个源文件都出现 / 提到的文件都存在 / 每份索引不超一屏 / 与代码同 commit
- 索引失真时以代码为准重建，禁止打补丁

10. 验收清单（每个里程碑跑一遍）
- [ ] main 能一键跑起来
- [ ] 停车场里没有偷偷混进 v1 的功能
- [ ] 没有平行宇宙（重复实现）
- [ ] 没有 import 白名单之外的引用
- [ ] 索引三层齐全且未失真
- [ ] 删除清单：本月至少删过一样东西

11. 读取分级（自动导入标准）
| 级别 | 文件 | 什么时候进 AI 工作集 |
|---|---|---|
| L1 自动 | AGENTS.md、根 README | 每次会话开头必读 |
| L2 条件自动 | 本次改动碰到的模块 README | 检测到有文件变更时随模块加载 |
| L3 手动 | charter / parking-lot / log / dependencies | 规划功能、审批、排障时才 @ 进来 |

- 自动加载总量预算 <2000 字。超了说明索引太肥，该减肥的是文档不是预算
- 禁止把整个 docs/ 一次性塞给 AI
- 新会话固定开场白："读 AGENTS.md 和根 README，报告索引是否失真"

12. 文档编制与自动整合
- 固定编制，超编即 bug：AGENTS.md / README.md / docs/charter.md /
  docs/parking-lot.md / docs/dependencies.md / docs/log.md / 每模块 README
- 编制之外每多一个 .md = 一个 bug，本次 commit 内合并或删除
- 运行日志只准 docs/log.md 一个文件，散落的想法先记这里，不许开新文件
- log.md 超 300 行 → 归档为 docs/archive/YYYY-MM.md，原文件清空重写

文档整合清单（每个里程碑，AI 执行）
- [ ] 揪出编制外 .md → 合并回所属文档或删除
- [ ] 同一主题散落多份 → 合并，一个主题只留一个家
- [ ] log.md 到龄归档
- [ ] 每份文档复查仍不超一屏
- [ ] 整合动作与代码同一 commit

13. AI 产出标准（不许应付）
- 给建议必须三件套：怎么做 + 为什么 + 不这么做会怎样，缺一项视为没答
- 被问"哪个好"必须给明确判断，禁止各打五十大板
- 发现方案有问题当场直说，禁止顺着你说
- 不知道就说不知道，禁止编造；不确定就标注不确定
- 不许以"个人项目/学习项目够用"为由降标准，标准只有一个
- 每次产出前自检：这是我能给的最好答案吗？还是在糊弄？

14. 架构决策记录（ADR）
- 每个改变结构/方向的重大决定，写一页 docs/adr/NNNN-标题.md
- 固定四段：背景 / 决定了什么 / 为什么（考虑过哪些替代方案）/ 什么条件下可以推翻
- 看到怪代码不敢动时，先查 ADR 再动刀

15. 技术债登记册
- docs/debt.md：主动欠的债当场登记——欠了什么、为什么欠、计划何时还
- 债进债册，功能进停车场，两者不许混住
- 每个里程碑还一笔最小的债

16. 项目配额（1+1）
- 同时进行 1 主 1 副，写死。想开第三个，先给现存项目办结业：
  跑完第 10 节验收清单 → 归档或删除 → 才准开新坑
- 结业的进作品集，没结业的只算学费
